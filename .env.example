# Provider: gemini | fake
# "fake" runs the whole pipeline offline with a deterministic stub client —
# no API key needed. Useful for reviewing this repo without provisioning keys.
LLM_PROVIDER=gemini

# Gemini — free tier is enough for this task.
# Get a key at https://aistudio.google.com/apikey
GEMINI_API_KEY=
# Free-tier daily quotas differ wildly per model: gemini-3.5-flash allows
# only 20 requests/day, which is not enough for an 18-row inbox plus retries.
GEMINI_MODEL=gemini-3.1-flash-lite

# 0 = thinking off (default, cheapest). "auto" lets the model decide.
GEMINI_THINKING_BUDGET=0

# Pipeline tuning
MAX_CONCURRENCY=4
MAX_ATTEMPTS=4

# Gemini free tier is 5 requests/minute/model. Raise it on a paid key;
# set 0 to disable client-side pacing entirely.
REQUESTS_PER_MINUTE=5
REQUEST_TIMEOUT_S=60
